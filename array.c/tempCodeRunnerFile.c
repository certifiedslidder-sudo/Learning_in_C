for(int i =0; i<=6;i++){ // index will go one le
    //     if(max< arr[i]){
    //         max = arr[i];
    //     }
    // }
    // for(int i =0; i<=6;i++){ // index will go one le
    //     if(arr[i] != max && smax< arr[i]  ){
    //         smax = arr[i];
    //     }
    // }
    // //MAX = 90;