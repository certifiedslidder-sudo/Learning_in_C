// rotate a given array 'a' by k steps, where k is non- negative.

//note = k can be greater than n as well where n is the size of array'a'.

//steps 1- k=k%n               2- reverse(arr,0,n-1)              3- reverse(arr,0,k-1)        4- reverse(arr, k, n-1)


#include<stdio.h>
int main(){
    

    
}
           


                                                                                     // if k>n;    k=k-n... if still k>n then again k=k-n...
                                                                                     //better option ,       k=k%n       =C0RRECT WAY      